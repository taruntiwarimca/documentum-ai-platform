# Support-matrix data for Content Server 23.4, consumed by
# mcp/compatibility-mcp's `compatibility.check_stack` tool (ADR-014).
#
# NOTE: this is illustrative data for the local target-architecture build,
# not sourced from OpenText's actual published compatibility/release
# documentation (target architecture §15) — replace with real entries
# before this is used against a real environment. `status: SUPPORTED` here
# means "this project's own ADRs accepted this as the target stack," not
# an independently verified OpenText certification.
component: content-server
version: "23.4"
compatible_with:
  oracle: ["19c"]
  tomcat: ["10"]
status: SUPPORTED
