# Architecture

The full target architecture lives in the POC repo, not here, to keep one source of truth:

```
../../../documentum-ai-platform-poc/docs/architecture/TARGET_ARCHITECTURE.md
```

Formalized as ADR-014 through ADR-020 in `../../../documentum-ai-platform-poc/adrs/`.

This repo's own `docs/architecture/` holds one implementation-specific document: `REAL_INFRA_CUTOVER.md` — what changes in each agent when real Oracle/Documentum infrastructure becomes reachable (it isn't yet).
