# INFRA-GROK

Not detailed with a specific responsibilities/tools list during target-architecture design. Inherits scope from ADR-001 and the existing tool allowlist in the POC's `config/mcp-tools.yaml`:

```
infra.precheck, infra.service, infra.filesystem, infra.network
```

Flagged as open work — see target architecture §6.7 and §16.5.
