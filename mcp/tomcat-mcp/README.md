# TOMCAT-MCP

Backs APP-GROK (`../../agents/application-server/`). Enforces the Java compatibility matrix gate before any deploy proceeds. A Documentum Automation Worker (see `../../automation/ansible/tomcat/`) resolves each typed call to the exact commands.

Design source: target architecture §5, §9, ADR-014, ADR-016.
