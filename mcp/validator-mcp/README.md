# VALIDATOR-MCP

Backs VALIDATOR-GROK (`../../agents/validator/`). Tool surface (unchanged from the POC): `validate.component, validate.golden_transaction, validate.evidence`. Runs the suites in `../../validation/`.

Design source: target architecture §6.7, §9.
