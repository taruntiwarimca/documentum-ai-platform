# CONTENT-SERVER-MCP

Backs CS-GROK (`../../agents/content-server/`). Exposes the typed tool surface listed there; a Documentum Automation Worker (see `../../automation/ansible/content-server/`) resolves each typed call to the exact commands.

Design source: target architecture §9, ADR-016.
