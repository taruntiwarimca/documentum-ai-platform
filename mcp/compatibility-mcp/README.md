# COMPATIBILITY-MCP

Backs COMPATIBILITY-GROK (`../../agents/compatibility/`). Resolves the support matrix and Java compatibility matrix against the knowledge base in `../../compatibility/`, returning APPROVED/BLOCKED before any install proceeds.

Design source: target architecture §5, §9, ADR-014, ADR-016.
