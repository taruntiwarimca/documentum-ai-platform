# Tomcat automation playbooks

The Documentum Automation Worker layer behind `mcp/tomcat-mcp/`. Runs only after the Java compatibility matrix gate (see `../../../agents/application-server/`) resolves APPROVED.

Design source: target architecture §5, §9, ADR-014, ADR-016.
