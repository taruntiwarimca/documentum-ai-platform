# SECURITY-MCP

Backs SECURITY-GROK (`../../agents/security/`, not yet detailed — see target architecture §6.7). Owns secret-reference resolution: `GROK → SECRET REF → VAULT → execution worker`.

Design source: target architecture §6.7, §14, ADR-006.
